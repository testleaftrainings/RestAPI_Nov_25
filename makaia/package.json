{
  "name": "makaia",
  "version": "1.0.0",
  "description": "Kindly create **\"secret.properties\"** file under the **\"src/test/resources/\"** mentioned location.",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "generate": "npx allure generate allure-results --clean",
    "publish": "npx allure serve",
    "report": "npx allure generate allure-results --clean && npx allure serve"
  },
  "devDependencies": {
    "allure-commandline": "^2.36.0"
  }
}
